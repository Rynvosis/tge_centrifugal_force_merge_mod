# Extends vanilla ship_uses_titan_components from
# Stellaris/common/scripted_triggers/07_scripted_triggers_ships.txt to include
# our new marauder_doomstar — without this, PERDITION_BEAM_TITAN's potential
# gate (`ship_uses_titan_components = yes`) rejects the design and the doomstar
# spawns weaponless. Loaded with zz_ prefix so it wins the duplicate-trigger
# resolution.

ship_uses_titan_components = {
	OR = {
		is_ship_size = titan
		is_ship_size = bio_titan
		is_ship_size = cosmo_crisis_titan
		is_ship_size = large_fallen_stinger
		is_ship_size = cosmo_crisis_stinger
		is_ship_size = marauder_doomstar
	}
}
