# Version last updated: v4.2.3
# Overwrite: wg_plunder (expanded with gk_raiding pop-only peace behavior)

#####################
# RAIDING WAR       #
#####################
# General raiding: pop-focused peace outcome
# Bucket-based escalation cap
wg_plunder = {
	casus_belli = cb_despoliation
	hide_if_no_cb = yes
	total_war = no

	galactic_empire_joins_defender = {
		from = { is_galactic_emperor = yes }
		imperial_authority >= 100
	}

	surrender_acceptance = -75

	potential = {
		has_total_war_cb = no
		gk_can_use_raiding_cb = yes
		has_policy_flag = gk_focus_plunder
		NOT = { is_in_federation_with = from }
		NAND = {
			is_galactic_emperor = yes
			from = { is_galactic_community_member = yes }
		}
	}

	possible = {
		custom_tooltip = {
			fail_text = has_claimed_all_systems
			from = {
				is_country_type = default
				any_owned_planet = {
					is_colony = yes
					root = {
						NOT = { has_claim = prev.solar_system }
					}
				}
			}
		}
		custom_tooltip = {
			fail_text = cannot_use_against_emperor
			OR = {
				from = { is_galactic_emperor = no }
				is_galactic_community_member = no
			}
		}
		custom_tooltip = {
			fail_text = pax_galactica_active
			NAND = {
				is_active_resolution = resolution_emperor_pax_galactica
				is_galactic_community_member = yes
				from = { is_galactic_community_member = yes }
			}
		}
	}

	on_accept = {
		custom_tooltip = wg_plunder_accept_tooltip
		hidden_effect = {
			country_event = {
				id = gk_raiding_pops.310
				scopes = { from = from }
			}
		}
	}

	ai_weight = {
		weight = 100
	}
}
